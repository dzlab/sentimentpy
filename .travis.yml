language: python
python:
  - "2.7"
virtualenv:
  system_site_packages: true
# install dependencies
before_install:
  - sudo apt-get install -qq python-numpy python-scipy
  - sudo pip install python-dateutil
  # intall the 'guess language' module
  - sudo pip install -e 'git://github.com/dsc/guess-language.git#egg=guess-language'
install: 
  # - python setup.py dev
# run tests
script: nosetests
