language: python
python:
  - "2.7"
virtualenv:
  system_site_packages: true
# install dependencies
before_install:
  - sudo apt-get install -qq python-numpy python-scipy
install: 
  - python setup.py dev
# run tests
script: nosetests
